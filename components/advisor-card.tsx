'use client'
import Link from 'next/link';import {AdvisorMatch} from '@/types';import {ArrowRight,Check,MapPin} from 'lucide-react';import {motion} from 'framer-motion'
export function AdvisorCard({advisor}:{advisor:AdvisorMatch}){return <motion.article initial={{opacity:0,y:8}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-30px'}} transition={{duration:.38,ease:'easeOut'}} className="card p-6 transition hover:-translate-y-0.5 hover:shadow-[0_12px_35px_#10b9812c] border-green-200"><div className="flex items-start justify-between gap-4"><div className="flex gap-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-full border border-green-300 bg-green-50 font-serif text-lg text-green-700">{advisor.initials}</div><div><h3 className="font-serif text-xl font-semibold">{advisor.name}</h3><p className="mt-1 text-xs text-gray-500">{advisor.specialties.join(' · ')}</p></div></div><div className="text-right"><b className="text-xl text-green-600">{advisor.score}%</b><div className="text-[10px] uppercase tracking-wider text-gray-500">match</div></div></div><div className="my-5 h-1 overflow-hidden bg-gray-100"><motion.div initial={{width:0}} whileInView={{width:`${advisor.score}%`}} viewport={{once:true}} transition={{duration:.65,delay:.15}} className="h-full bg-green-500"/></div><p className="text-sm text-gray-600">{advisor.reason}</p><div className="mt-5 flex flex-wrap gap-2 text-[11px]"><span className="border border-green-200 px-2 py-1 text-green-700">{advisor.years}+ years</span><span className="border border-green-200 px-2 py-1 text-green-700">{advisor.licensedStates} states licensed</span>{advisor.fiduciary&&<span className="flex items-center gap-1 border border-green-200 px-2 py-1 text-green-700"><Check size={11}/> Fiduciary</span>}</div><div className="mt-4 flex items-center gap-1 text-xs text-gray-500"><MapPin size={13}/>{advisor.location}</div><div className="mt-6 flex gap-3"><Link className="btn btn-light flex-1" href={`/advisors/${advisor.id}`}>View Profile <ArrowRight size={14}/></Link><Link className="btn btn-dark flex-1" href={`/advisors/${advisor.id}/connect`}>Connect</Link></div></motion.article>}
